[pytest]
testpaths = scripts
python_files = test_*.py
addopts = -p no:doctest -p no:cacheprovider -m "not integration"
markers =
    integration: tests requiring external services or writable vector DB
norecursedirs = .git frontend/node_modules data logs temp_uploads __pycache__ pytest-cache-files-*
